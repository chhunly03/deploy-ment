name: deploy

on:
  push:
    branches:
      - main

jobs:
  notify-push:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Telegram (push)
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="📌 Code pushed by *${{ github.actor }}* to *${{ github.repository }}* on branch *${{ github.ref_name }}* with commit message: _${{ github.event.head_commit.message }}_" \
          -d parse_mode="Markdown"

  deploy:
    needs: notify-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Trigger Vercel Deployment
        run: |
          if [ -z "$VERCEL_HOOK_URL" ]; then
            echo "❌ ERROR: VERCEL_HOOK_URL is not set."
            exit 1
          fi
          echo "🚀 Triggering Vercel deploy..."
          curl -s -X POST "$VERCEL_HOOK_URL"
        env:
          VERCEL_HOOK_URL: ${{ secrets.VERCEL_HOOK_URL }}

  notify:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Notify Telegram
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d text="🚀 Deployment requested for *${{ github.repository }}* on branch *${{ github.ref_name }}*." \
          -d parse_mode="Markdown"
